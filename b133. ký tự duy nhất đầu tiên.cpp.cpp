#include <bits/stdc++.h>

using namespace std;

int main()
{
    map<string,int>mp;
    int n,i,dem; cin >> n;
    string s; getline(cin,s);
   for(i=0;i<s.length();i++){
        dem=0;
    if(s[i]!=' '){
        cout << s[i] << endl;
    }
   }

    return 0;
}
